# Gridwork CLI

The Gridwork CLI is the bootstrapper for installing a Gridwork factory into a repository.

## Commands

```bash
gridwork init
```

`gridwork run` is not part of v1.

## Phase 0 Status

The CLI package exists and recognizes `init`, but the real installer is implemented in later MVP phases.
