# Gridwork Factory <version>

```text
tag = factory-v<version>
source = <owner/repo>
source_commit = <sha>
release_channel = stable
prerelease = false
```

## Install

```bash
npx gridwork init --factory-version <version> --source <owner/repo>
```

## Notes

- Review generated assets before publishing.
